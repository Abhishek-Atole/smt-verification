# Windows batch files must be CRLF or cmd.exe parsing can choke on certain
# constructs (delayed expansion blocks, multi-line `for /f` bodies).
*.bat text eol=crlf
*.cmd text eol=crlf

# PowerShell scripts likewise prefer CRLF on Windows hosts.
*.ps1 text eol=crlf
