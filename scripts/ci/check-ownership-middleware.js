#!/usr/bin/env node
// Verifies every session-id route has requireOwnership middleware.
// Phase 0 stub — exits 0 so CI passes until Phase 2 ownership enforcement is built.
console.log("check-ownership-middleware: stub, passing");
process.exit(0);
