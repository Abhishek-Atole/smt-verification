#!/usr/bin/env node
// Verifies JWT cookies are set with httpOnly, secure, sameSite=strict.
// Phase 0 stub — exits 0 so CI passes until Phase 2 cookie hardening is built.
console.log("check-cookie-security: stub, passing");
process.exit(0);
