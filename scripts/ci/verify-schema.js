#!/usr/bin/env node
// Verifies Drizzle schema matches the live database (no pending migrations).
// Phase 0 stub — exits 0 so CI passes until DB is available in CI environment.
// Replace with: drizzle-kit check or schema diff logic before production.
console.log("verify-schema: stub, passing");
process.exit(0);
